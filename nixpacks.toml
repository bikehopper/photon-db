# Only the go provider will be run
providers = ["java"]

[phases.install]
dependsOn = ['setup']
cmds = ["sh coolify/install.sh"]

[phases.build]
dependsOn = ['install']
cmds = ["echo 'yay'"]

[phases.start]
dependsOn = ['build']
cmds = ["sh coolify/run.sh"]